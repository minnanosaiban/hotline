# hotline

「ＥＮＥＯＳの内部通報制度をめぐる訴訟について」のサイト。公開先は **https://minnanosaiban.github.io/hotline/**（GitHub Pages）。

元は MkDocs Material 製の `hotline` リポジトリ（`kabuka` を経て現在は `tomo` にリポジトリ名変更）にあったものから、株価分析（blog）と運営者ページを除いて切り出した。旧サイトの内容は今も `tomo`（**https://minnanosaiban.github.io/tomo/**）としてそのまま公開されている（ツール紹介・ポートフォリオサイトへ改装済み）。2026-09-23 に、このリポジトリ（旧 `eneos-hotline`）が正式に `hotline` の名前とURLを引き継いだ。

**ビルドは Zensical が主、MkDocs 1.6.1 は予備。** Python フックもプラグインも使わない作りなので、どちらでも同じ見た目になる（下の「検証」）。

## 動かし方

初回だけ、仮想環境を作って Zensical を入れる（バージョンは `requirements.txt` で固定）。

```
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt
```

| 何をする | 方法 |
|---|---|
| ローカルで見る | `serve.bat`（`http://localhost:8000/`。開くと `/hotline/` に移る。**動かしたまま `build.bat`・`deploy.bat` を実行しない**: どれも同じ `site/` を作り直すので、`site/` が空になって全ページが 404 になることがある（2026-09-22 に発生。その場合は `serve.bat` の画面を閉じて、開き直す）。2026-09-23、`parts/` を `docs/trial2024/.parts/` へ移してからは、`.parts/` の中だけを直しても `serve` が普通に気づく（以前は `docs/` の外にあったため気づかなかった）） |
| 公開用ファイルを作る | `build.bat`（`site/` に出力） |
| **公開する** | **`deploy.bat`**（ビルド確認 → コミット・push → 公開の完了を確認、まで） |

`--8<--` での本文の取り込みは「コマンドを実行したフォルダ」基準なので、必ずこのフォルダで動かす（`.bat` は `cd` してから動かしている）。
間違えたときは、本文が黙って抜けるのではなく `Snippet at path … could not be found` で止まる（`check_paths: true`）。

## 公開（GitHub Pages）

**`master` に push すると、GitHub Actions（`.github/workflows/pages.yml`）が Zensical でビルドして公開する。** 普段は **`deploy.bat` をダブルクリック**すればよい（`git push` だけでも同じ）。
進み具合は、リポジトリの Actions タブで見られる（1〜2分）。公開後の確認は、上の URL を開く。

- **`deploy.bat` の流れ**: ① ビルド確認（失敗したら push しない）→ ② `git add .`・コミット・`git push -u origin master`（**強制 push はしない**。ほかのリポジトリの `deploy.bat` と違い、`gh-deploy` や `--force` は使わない）。送るものが無い（GitHub にまだ無いコミットが無い）ときは push せず、次へ進む。push が通信エラー（`Empty reply from server` など）で失敗したときは、5秒おきに最大3回まで再試行する（2回目からは HTTP/1.1）。3回とも失敗したら、コミットは手元に残したまま止まる
  → ③ `scripts/wait_deploy.ps1` が、GitHub Actions の実行（このコミットの分）を待って、成功か失敗かを表示する（GitHub CLI の `gh` が要る。無い・サインインしていないときは、待たずに Actions の URL を出すだけ）。
    **止まったときは自動でやり直す（2026-09-22）**: 通常は約30秒。GitHub Pages が最後の `actions/deploy-pages` で止まることが実際にあった（run 35623135584。5分以上 `in_progress` のまま。GitHub 側は全体が正常表示）ので、
    実行開始から4分（`-TimeoutSec 240`）で「止まった」とみなし、`gh run cancel` → 止まるのを待つ → `gh run rerun` を**1回だけ**自動で行う（`-Retries 1`）。やり直しの間も、公開中の旧版はそのまま見えている。
    起動した時点ですでに長く止まっている実行（`deploy.bat` をもう一度走らせたとき等）は、待たずにすぐ取り消して再実行する。終了コード: 0＝公開済み／確認できなかった（`gh` なし・未ログイン・実行が見つからない・通信不能）、1＝失敗、2＝やり直しても終わらない（`deploy.bat` が Actions の URL を出して止まる。旧版は公開されたまま。しばらくしてもう一度 `deploy.bat`）。
    旧 hotline の `deploy.bat` は `mkdocs gh-deploy` で `gh-pages` に push するだけで公開の完了を待たないため、同じ場所で止まっても画面には出ない（`gh-pages` 方式でも、push のあとに GitHub 内部で `Upload artifact` → `Deploy to GitHub Pages` が走る。実行履歴の「pages build and deployment」。旧 hotline の直近60回は全部成功、最長112秒）。この待機工程は新しい側で足したもの。
  → ④ `scripts/indexnow_ping.ps1` で IndexNow に通知（失敗しても止まらない）。`.venv` が無い PC では、作り方を表示して止まる
- Actions は `requirements.txt` の固定版で入れるので、手元と同じ結果になる（公開された66ファイルを手元の `site/` と比べて、Windows の手元ビルドが HTML の改行を CRLF で出す点（公開側は LF）を除き、すべて一致することを確認した）
- 設定は Settings > Pages > Source = 「GitHub Actions」。ブランチ（gh-pages）は使わない
- 公開を止めたいときは、Settings > Pages でサイトを非公開にする（または Actions のワークフローを無効にする）

### 予備: MkDocs でビルドする

```
pip install -r requirements-mkdocs.txt
python -m mkdocs serve       # http://localhost:8000/hotline/
python -m mkdocs build
```

- Material for MkDocs は **2026-11-05 で更新終了**（保守モード）。MkDocs 2.0 はプラグイン機構ごと作り直しで、この構成とは互換がない。`requirements-mkdocs.txt` は `mkdocs<2` に固定してある
- Zensical は 0.0.x（1.0 前）。**上げるときは `requirements.txt` を書き換えて、下の「設計ルール」の落とし穴に触れていないか、ビルド結果を見比べてから**

## 元の hotline との違い

- **株価分析（blog/）を除いた**。nav・ブログ用CSS（`10-blog-reset.css` は廃止。判決ページの h2 と `.repo-link` だけ `11-trial.css` の先頭へ統合）・`docs/blog/` がなくなった
- **運営者ページ**: フッターの「運営者について」のリンク先は `mkdocs.yml` の `extra.about_url` の1か所（今は `https://minnanosaiban.github.io/tomo/`）。2026-09-23 に、`tomo` の Home を写した `about/` も並行して置いた（`robots: noindex, nofollow`。フッターからはリンクしていない）
- **NotebookLM の音声解説は載せない**（とりあえず。判決ページ末尾のセクションと、旧 eneos ページのカードを外した）。音声ファイル（`*.m4a`・`*.wav`、約100MB）も持ち込んでいない
- **目次（Toc）を完全に出さない**。右のサイドバーだけでなく、スマホのメニュー内の目次も。Material の `partials/toc.html` を空にして実現（右カラムは CSS でも `display: none`）
- **検索窓を出さない**（`plugins: []` と、`overrides/partials/header.html`）
- **トップへ戻るボタン（`navigation.top`）は廃止**（2026-09-22。ユーザーがスクリーンショットで指摘：「画面が崩れました」）。既定は Material の classic テーマの挙動で、上にスクロールした際に画面上部中央へ固定表示され、本文の列（32〜40rem、中央寄せ）と重なって読めなくなる。位置を右下・左下と動かしても、今度は裁判文書ページのサイドノート（`.sn-note`。本文列の右余白に出る）と衝突するため、位置の調整では直らないと判断し、`mkdocs.yml`の`theme.features`から`navigation.top`を削除した（要素自体がHTMLに出力されなくなる。ビルド後の`grep -c 'md-top' site/trial/index.html`が`0`であることを確認済み）
- **「裁判文書公開」タブは、提訴年ごとのドロップダウン（2021年提訴／2024年提訴／2025年提訴）にした**（2026-09-22）。ユーザーの依頼「Bootstrapのドロップダウンのように、タブの下に複数ページをぶら下げたい」に対応。**Zensical 0.0.63 の既定テンプレート（`partials/tabs-item.html`）は、タブに子ページが複数あっても最初の1件（`navigation.indexes` があれば index 優先）だけをリンクにし、残りはタブバーから消える**（mkdocs-material の「タブにホバーでドロップダウン」相当の分岐が無い。`.venv/Lib/site-packages/zensical/templates/partials/tabs-item.html` で確認）。そのため `overrides/partials/tabs-item.html` にこのファイルをコピーして、子が2件以上のときだけ `<ul class="md-tabs__dropdown">` を描く分岐を追加した（子が1件だけの既存タブ＝Home・株主総会質疑応答は従来どおり）。見た目（CSS）は `docs/css/02-layout.css`「タブのドロップダウン」に追加し、`.dmdmenu`（14-doc-accordion.css。書面の.md/.pdfボタンのメニュー）と同じ浮き出るメニューの部品（背景・枠線・box-shadow・角丸）を流用した。ホバーでしか開かないため（PC向け）、ホバーの無いモバイルでも3ページ間を行き来できるよう、各ページの hero-band 直下に相互リンク（`.trial-year-nav`。現在地は `<b>` でリンクにしない）を追加した（CSS は `03-typeset.css`）。**ローカルサーバ（`zensical serve`）は `overrides/` に新規ファイルを追加しても検知せず、再起動するまで反映されなかった**（既存ファイルの変更は反映される。原因未調査）。`2021年提訴`（`docs/trial2021/index.md`）・`2025年提訴`（`docs/trial2025/index.md`。のちに `trial2026/` へ改名）は、当初は内容が無い「作成中」だけのページとして作った（`robots: noindex, nofollow` で検索結果からは除外。`2026年提訴`は今もそのまま。`2021年提訴`には、その後、判決文と「判決の概要」を置いた）。`2024年提訴`は既存の `trial/index.md` をそのまま使っていたが、2026-09-23 に `trial2024/index.md` へ改名した（旧 `trial/index.md` は `trial2024/` への転送ページ）。
  **2026-09-22（その3）**: ユーザーが実機（スクリーンショット）で確認したところ、ドロップダウンが出ないと指摘された。原因はホバー方式そのもの――**タップ操作では:hoverが発火せず、ホバーせずクリックした場合はその場でタブの先頭ページへ遷移してしまい、ドロップダウンを見る機会が無い**。参考として貼られたスクリーンショット（MDwiki。Bootstrapの標準ドロップダウン＝クリックで開閉、への字マーク付き）に合わせ、**ホバーからクリック開閉へ作り直した**：`overrides/partials/tabs-item.html` のトリガー `<a>` に `.md-tabs__link--dropdown`（クラス名でJSがフックする）と `bi-chevron-down` のへの字（他のアコーディオンの開閉マークと同じアイコン）を追加。開閉は `docs/js/custom.js` に追加した数十行のバニラJSが行う：クリックで親 `<li>` に `is-open` を付け外し（`e.preventDefault()` でトリガー自体の遷移は止める。JSが読めない場合は先頭ページへのリンクとして後退動作）、他のドロップダウンが開いていれば閉じる、ドロップダウンの外側をクリック／Escapeキーで閉じる。デザインは「シンプルに、吹き出しのように」というユーザーの指示（つまみ三角付きの白い角丸の箱の画像を提示）に合わせ、`.md-tabs__dropdown::before` で上端に回転させた正方形（吹き出しのつまみ）を描いた。**実装中、思わぬハマりどころが2つあった**: ①z-indexをいくつ上げてもドロップダウンが本文の下に隠れて見えない現象は、`elementsFromPoint`で調べたところ z-index ではなく **Zensical既定の `.md-tabs`・`.md-tabs__list` が持つ `overflow: auto` と `contain: content`**（横スクロール用の指定）が吹き出しを縦方向にクリップしていたのが原因だった。`docs/css/02-layout.css` でこの2つのセレクタに `overflow: visible !important; contain: none !important;` を当てて解決（モバイル幅では横スクロールを維持するため `overflow-x: auto` を別途復元）。②`overflow-y` だけを個別に上書きしても効かず、`overflow`（ショートハンド）で上書きする必要があった（原因不明。ショートハンドと個別指定が絡む何らかのカスケードの癖と思われる）。
  **2026-09-22（その4）**: ユーザーが実機のスマホで確認したところ、まだ「うまく行かない」との指摘。**CSSの `overflow-x:auto` と `overflow-y:visible` は仕様上共存できず（片方が visible でなければもう片方も自動的に non-visible 扱いになる）、その3の `overflow-x:auto !important` の下で `overflow-y` は結局 auto のままだった**（モバイル用メディアクエリの中だけの話）。加えて、スマホの狭い画面では `position:absolute` の箱がタブの位置によっては画面右端からはみ出すこともあった。両方を一度に解決するため、`.md-tabs__dropdown` を **`position:absolute` から `position:fixed` に変更し、開くたびに `docs/js/custom.js` がトリガーの `getBoundingClientRect()` から top/left を計算してインラインstyleに置く方式**に作り直した（fixedなら祖先のoverflowの影響を受けない。leftは画面幅からはみ出さないようクランプする）。これに伴い、その3で入れた `.md-tabs`/`.md-tabs__list` への `overflow`/`contain` の上書きは不要になったので削除し、実装をシンプルに戻した。**それでも直接の原因はクリックの当たり判定だった**: ブラウザの実機で見た目上ドロップダウンが開かず、自動テスト環境（このBrowserペイン）でも座標クリックで再現できた――トリガーの `<a>` だけにクリックリスナーを付けていたため、タップ位置が `<a>` の文字部分から数px外れる（周囲の余白・への字アイコンの脇など）と反応しなかった。**リスナーを `<a>` から親の `<li class="md-tabs__item--dropdown">` 全体に付け替え**、タップ判定の当たり外れを解消した（中のドロップダウン項目のクリックは、開閉に関与させず通常のリンク遷移に任せる分岐を追加）。あわせて、ユーザーの指摘「ドロップダウンの幅はもう少し狭くても大丈夫」で `min-width` を `9.5rem`→`6.5rem` に縮小した。
  **2026-09-22（その5）**: デプロイ後、ユーザーから「ドロップダウンが効かなくなりました」。ライブサイトを直接（`javascript_exec`でCDN配信のHTML/JSを取得し、自動クリックで）確認したところ機能はしていたため、まずキャッシュ（GitHub Pagesは`max-age=600`）を疑ってユーザーにハードリフレッシュを依頼したが、「確認しましたが効きません」との返答。**実際の原因はその4で入れた「リスナーを`<li>`に付け替え」自体だった**: iOS Safariは非アンカー要素（li/div）のclickイベントが確実に発火しない既知の癖があり、これがユーザーの端末で症状として出ていたと判断（このBrowserペインはmac/Windows Chromeベースのため再現しなかった）。**リスナーをトリガーの`<a>`へ戻し**、その4で問題になった「当たり判定の狭さ」は別の方法で解決した: `.md-tabs__link--dropdown`を`position:absolute; inset:0`で親いっぱいに広げる案を最初に試したが、それだと`<a>`が通常のflowから外れて`<li>`の幅計算に寄与しなくなり、**タブ行全体が潰れて隣の「株主総会質疑応答」と文字が重なる新しい事故**になった（実機スクショで発覚）。正しい修正は、Material既定の`.md-tabs__item`が持つ高さ(`2.4rem`)とパディング(左右`.6rem`。コンパイル済みCSSで確認)を`<li>`から`<a>`側へそのまま移し、`<a>`を`height:100%`・横は通常のflowのままにすること（`<li>`の幅計算は今までどおり`<a>`の中身に任せる）。**あわせてユーザーの指摘「スマホのハンバーガーメニューは不要」に対応**: 全ページが`hide: navigation`を指定しており、Zensical既定の`base.html`は`.md-sidebar--primary`（ドロワーの中身）に`hidden`属性を常に付けるため、ハンバーガーボタンを押しても何も開かない（実機で`hidden=""`を確認済み）ことが分かり、`overrides/partials/header.html`からボタンを削除した（チェックボックス`#__drawer`自体は`base.html`側に残るが、開くボタンが無いので無害）。**このBrowserペインでのモバイルプレビュー（`resize_window preset:"mobile"`）はdevicePixelRatio 2をエミュレートしており、`getBoundingClientRect()`で得たCSS px座標をそのまま`computer`ツールの`coordinate`に渡すと実際のクリック位置とずれる（`ref`ベースのクリックでも同様にずれることがあった）**ことも分かった。デスクトッププリセット（DPR 1.25、ずれ無し）での`ref`クリックは終始安定して成功しており、この座標ずれはテストツール側のモバイルエミュレーション特有の癖で、実機の指タップには当てはまらないと判断（実機での再確認をユーザーに依頼）。
  **2026-09-22（その6・最終形）**: 実機で再確認したユーザーから「ウィンドウズデスクトップでも、ドロップダウンが効きません」。**この時点でようやく、Windows Chromeという最も一般的な環境ですら動かないことが判明**——モバイル・iOS Safariの癖の話ではなく、独自クリックJSの仕組みそのものが、原因不明のまま複数の実環境で機能していなかった。切り分けのため、シークレットウィンドウ（拡張機能の影響を除外→まだ動かない）、DevToolsコンソールのエラー確認（ユーザーが貼ったスクリーンショットに写っていたのは`BlockingPageContent.js`等、**拡張機能由来のログで、サイト自身のエラーではないと判明**）と進めたが、最終的な原因はユーザーの環境から直接コンソールを実行してもらう前に特定できなかった。**ユーザーの「難しいなら無理に実装しなくてもいい」を受け、原因追及をやめて設計そのものを見直した**: 独自のクリックJS（`is-open`クラスの付け外し・`position:fixed`のJS計算配置）を全廃し、**このサイトの書面一覧などで実績のある`<details>/<summary>`（ブラウザ自身が開閉を担うHTML標準の仕組み）に作り直した**。`overrides/partials/tabs-item.html`は`<a href><ul></a>`ではなく`<details class="md-tabs__dropdown-details"><summary>…</summary><ul>…</ul></details>`を出力し、`docs/js/custom.js`は「他のドロップダウンが開いていたら閉じる」「外側クリックで閉じる」という**補助（無くても開閉・遷移という核心機能には影響しない）**だけに縮小した。CSSは、その5の`position:fixed`をやめて`position:absolute`に戻し（`<details>`がネイティブに開閉するので、JSでの座標計算がそもそも不要になった）、当たり判定を広げる工夫（`<li>`のpaddingを`<summary>`へ移す）はその5の対策をそのまま踏襲。**ここでまた「overflow-x:autoとoverflow-y:visibleは共存できない」バグを再発させかけた**（その4で見つけた同じ罠に、`<details>`化の書き直しで再び踏んだ）。モバイル用メディアクエリ内で`overflow-x/-y`を個別指定していたのが原因で、`.md-tabs`/`.md-tabs__list`に対する`overflow: visible !important`のショートハンド1本（メディアクエリ無し、全幅で共通）に統一し、除去した（3個のタブは375px幅でも折り返し不要なので、横スクロール自体が不要と判明——動作確認をご希望の場合は実機で）。**教訓**: 独自のJSクリック処理は、開発機のブラウザで何度動作確認しても、実際のユーザー環境（拡張機能・ブラウザ差・ネットワーク・原因不明の何か）で動かない可能性を排除できない。同じ機能でもブラウザネイティブな仕組み（`<details>`、`<a href>`等）に置き換えられるなら、その方が圧倒的に信頼できる。
- **裁判文書ページの並びを、新しい方が先（2026年提訴／2024年提訴／2021年提訴）に変更**（2026-09-22）。ユーザーからの新情報「新訴（GST会計処理疑惑）の提訴予定日は2026年12月1日」（従来把握していた「2025年12月1日頃」から更に延びた）を受け、プレースホルダーだった `docs/trial2025/` を **`docs/trial2026/` へフォルダごとリネーム**し、front matter・本文中の「2025年提訴」表記もすべて「2026年提訴」に直した。`mkdocs.yml` の nav・各ページの `.trial-year-nav` 相互リンク・`scripts/pdf_index.py` の `TRIAL_PAGES` を、いずれも2026年提訴が先頭に来る順序へ揃えた。また、ユーザーの指示で **`trial/index.md` 末尾にあった「表示確認用（見本・削除可）」の行（`sample-web.md.txt` のサイドノート表示見本）を `trial2026/index.md` へ移設**した（本文ファイル自体は `docs/trial/md/sample-web.md.txt` のまま、`--8<--` のパスは `mkdocs.yml` のあるフォルダ基準なので移動不要。参照している行だけを trial2026 側へ切り出した）。
- **裁判文書の3ページ（2021・2024・2025年提訴）の冒頭（hero-band）を統一した**（2026-09-22）。ユーザーの指示「上部は同じにして、年度を変える」に対応。3ページとも見出し（`# 訴訟資料公開`）とその下の説明文、Xシェアボタンの文言を `trial/index.md`（2024年提訴）と一字一句そろえた（従来は `trial2021`・`trial2025` の見出しがそれぞれ「2021年提訴」「2025年提訴」で、3ページの体裁がバラバラだった）。年度の違いは、hero-band の下の相互リンク行（`.trial-year-nav`。現在地が`<b>`）と、`toc-wrap doc-list` 内の節見出し（`.issue-lead.sec-title`。`trial/index.md`の「2024年提訴&lt;br&gt;判決の概要」にあたる行を`trial2021`・`trial2025`にも追加。ただし概要文がまだ無いので「年度」だけの1行にしてある）の2か所に寄せた。front matter に `seo_title`・`url`・`image`・`twitter_card` も揃えて追加した（`robots: noindex, nofollow` は据え置き。中身が未完成なので検索結果には出さない）。
  **2021年提訴に判決文を追加**（2026-09-22、続き）。ユーザーの依頼「2021年提訴のページに、2024年提訴の『裁判所』の項目のように、地裁・高裁の判決文を置く」に対応。ソースは `eneos-saiban/judgment2022.md`（前々訴＝令和３年（ワ）提訴、東京地裁2022-12-22判決・東京高裁2023-06-15判決。標題は「2022年12月判決」だが地裁判決は12/22で高裁は2023年6月）。eneos-saibanは独自マークアップ（裸の`<p>`・`</p>`なし・改行区切り）なので、`docs/trial/md/{tisai,kousai}.md.txt`（前訴＝2024年提訴を移した際の規則）と同じ形へ機械変換した：裸`<p>`→`<p class="doc">`、既存クラスの`<p class="padN...">`はそのまま、各ブロックを`</p>`で閉じて空行区切り、「第１」等の見出し（`pad1 hg-idt`単独ブロック）は1行に畳んで`<b>`で囲む。`<a name="...">`アンカーは原文のまま保持（今回は相互参照に使っていないが、将来のため）。変換はPythonスクリプトで機械的に行い、**全角・半角込みでタグを全部剥がした地の文が変換前後で一字一句一致することを別スクリプトで検証**（法律文書の一字一句の正確性を最優先し、手で書き写す方式は取らなかった）。ソース末尾の`裁判官　山田悠一郎`の直後に空行と`##`だけの孤立した行があり（内容の続きではなく、ファイルの末尾のゴミと判断）、これは変換対象から除外した。高裁判決文の中に、判決文そのものではない sphinx `{margin}` ディレクティブ（「原告補足」バッジ付きの脚注。原告＝ユーザー本人が、控訴理由書の記載を補足する３つの事実とＪＤＬＡ Ｅ資格へのリンクを書いたもの）が１か所あり、これは機械変換の対象外にして、`<p class="pad3 idt"><b>［原告補足］</b>　…</p>` という地の文の1段落に手で書き換えた（サイト側にまだ `.sidenote`／`.sn-note` の実例が無く、判決文の中で新しく浮動サイドノートの実装を検証するのは今回の範囲外と判断）。**PDF**: `eneos-saiban/_static/eneos_judgment_2022.12.22.pdf`（35頁・27.8MB、画像のみで文字層なし）・`eneos_judgment_2023.06.15.pdf`（4頁・0.47MB、同じく文字層なし）を、`scripts/add_pdf.py ... 裁判所 2022-12-22 東京地裁判決文 --shrink`／`... 2023-06-15 東京高裁判決文 --shrink`で `docs/pdf/` へ追加（A4・200dpi・1bitに縮小。1.7MB／0.28MBに）。**OCR文字層（`--ocr`）は付けていない**：このマシンの `.venv` に無かった PyMuPDF・Pillow はその場で `pip install` したが、`--ocr` が要る scan-ocr（yomitoku・torch、GPU/CPUで数十秒/頁の重い処理）はセットアップしておらず、今回は範囲外と判断した。あとから `python scripts/add_pdf.py <元PDF> 裁判所 <日付> <書面名> --shrink --ocr` を掛け直せば追加できる（`docs/trial2021/index.md` の `data-pdf` の値は変わらない）。`scripts/pdf_index.py` は、`docs/trial/index.md` 1本だけを見て「本文」リンク（PDFの行から裁判文書ページの該当行へ飛ぶボタン）を付ける作りだったため、提訴年ページが増えたことに合わせて `TRIAL_PAGES`（trial・trial2021・trial2025 の3本）を全部見るように直した（直さないと、2021年提訴のPDFの「本文」リンクが永久に付かなかった）。
- **裁判文書は、提訴年ごとのページ（`trial2026/`・`trial2024/`・`trial2021/` の各 `index.md`）に、書面ごとのアコーディオンで置く**。本文は `docs/<提訴年のフォルダ>/md/<id>.md.txt` に置いて `--8<--` で取り込む。生ファイルは静的に公開されるので、`.md` ボタン（コピー／ダウンロード）の元にもなる。以下の「高裁判決の根拠」「関係法令・規程、証拠」などの欄は、`trial2024/index.md` のもの
- **並びは「高裁判決の根拠 → 裁判所 → 被告（ＥＮＥＯＳ）側 → 原告（通報者）側 → 関係法令・規程、証拠」**（各欄の見出しは `<p class="t-toc-head bar-title doc-side">`）。
  **「高裁判決の根拠」**（見出しの `id="bunseki"`。2026-09-22 に「判決文の答え合わせ」から改名）は、今は**2つの行**（どちらも書面ではないので、PDF・`.md`・要約のボタンは付けない `data-plain` の行）:
  「契約書の問題を指摘する通報は、そもそも無かった、とは」（`bunseki-nakatta`）、「「海外消費税を支払う合意」をしていたと推認できる、とは」（`bunseki-goui`）。本文は、公開しない取り込み用の `docs/trial2024/.parts/bunseki-*.md.txt`（`--8<--` で取り込む）。
  **経緯**: 2026-09-21 に、判決文の記述と同じ訴訟記録の中の資料とを突き合わせる「照合」形式で全面改稿し、8行（`bunseki-senryaku`・`-yomikata`・`-riyu-1`・`-riyu-2`・`-iimawashi`・`-seito`・`-timeline`・`-zenbun`）にした → 2026-09-22、ユーザーの指示で中身を全削除し、タイトルだけの4行（`bunseki-saiyo`・`-teigi`・`-nakatta`・`-goui`）で作り直した（コミット `2cf23ca`。この行の中でしか使っていなかった CSS（照合カード `.ac-card` 一式・`.f-head`・タイムライン `.tl-c` 系など）も `11-trial.css` から削除。`DESIGN_SYSTEM.md` も更新済み）→ 2026-09-23、`saiyo`・`teigi` を削除し、`nakatta`・`goui` に本文を入れた（`268ebd8`）。旧8行の中身と CSS は、`2cf23ca` の直前のコミットに残っている。
  **作り直すときの設計メモ**（旧8行で決めたこと）: 1つの論点＝1つの照合にし、どの照合も「判決の記述（逐語引用）→ そのまま読むと → 記録を当てると →（必要なら）反対からの読み方（会社側・判決側に最も有利な読み）→ 言えること／ここから先は言えない」の同じ順で書く。**段のラベルは CSS の擬似要素ではなく本文のテキストで書く**（AI に読ませる読者が多く、擬似要素の文字は読み取られない）。**「ここから先は言えない」は全照合に置く**（読み手は、言い切られているかを本当かどうかの代用にするので、上限を自分で書いておくことが誤読対策になる。元になった分析は `G:\マイドライブ\eneos_pj\02-01_ENEOS新訴枠組み\03_前訴判決の誤読防止ガイド.md`）。批判だけの検証は割り引かれるので、正当と評価できる部分も同じ精度で置く。
  **「関係法令・規程、証拠」**（2026-09-22 新設）は、末尾の「原告（通報者）側」欄の下に置いた欄。**4つの行**（すべて本文は `docs/trial2024/.parts/horei-*.md.txt`）: 「公益通報者保護法に基づく指針（令和３年内閣府告示第１１８号）の解説」（`horei-shishin`、`data-plain`）、「公益通報ハンドブック　平成29年9月 発行　21頁」（`horei-handbook`。PDF のボタン付き。控訴理由書等が「公益通報ハンドブック」として引用している箇所）、「ＥＮＥＯＳ　コンプライアンスホットライン規程」（`horei-kitei`、`data-plain`）、「東京証券取引所　コーポレートガバナンスコード」（`horei-cg-code`、`data-plain`。原則２－５・補充原則２－５①の本文。控訴理由書がガバナンス・コードの全原則応諾を根拠に引用している条文）。
  判決文側のアンカー（2026-09-21 に、旧照合から判決文へ飛ぶために足したもの）は、今も判決文の中に残してある: `2025-kousai-judgment_sashikae`（高裁「原判決１０頁５行目冒頭から…次のとおり改める」）、`2025-tisai-judgment_kozo`（地裁「通報者個人のために設けられたものではない」）、`2025-tisai-judgment_mushikaeshi`（地裁「信義則に反するとまでいうことはできない」）。今の2行からは使っていない。
  その下の**「裁判所」**の欄に、東京地裁・東京高裁の**判決文の全文**（`tisai`・`kousai`）を、書面と同じ形（PDF・`.md`・要約）で置いた。判決文の該当箇所へのリンクは、同じページの中の飛び先（`#…`）になっている。
  各行の中の見た目（`.f-head`・引用・14px の文字など）は、`.accordion-body` の CSS で決まる。**本文中のリンクは、青い文字＋薄い下線**（2026-09-22。以前は、リンクの後ろに斜め上矢印のアイコン `<i class="bi bi-arrow-up-right">` を付けて目印にしていたが、「読みにくい」との指摘で `parts/` の98か所を全部外した。CSS は 11-trial.css 末尾。目次 `.f-jumpnav` とタイムラインの出典 `.tl-src` は、もとの薄い下線（border-bottom）のまま。引用の出典ラベル `.q-chip` の中は、灰色のまま下線だけ）。**新しく書くときは、矢印を付けない**。**本文の左右の端は、行の左右の端（区切り線の端・見出しの縦線）にぴったり揃えてある**（2026-09-22。ユーザーがスクリーンショットの赤線で指摘。もとは `.accordion-body` の左右 1rem（`05-card.css`。スマホでは既に0）と `.doc-body` の左右 0.2rem（`14-doc-accordion.css`）で、約26px 内側に入っていた。`.doc-acc .accordion-body` と `.doc-acc[data-plain] > .doc-body` の左右を 0 にした。入れ子アコーディオン `.card-accordion`（agm など）はそのまま。行のタイトル文字だけは、summary の余白（約4px）の分、内側のまま。全9行で、本文枠と行の左右のずれが0、はみ出す要素なし、をブラウザで実測）。 **2026-09-22（その2、user 指摘「行のタイトル文字も揃えてください。そのほかのアコーディオンも左端・右端を揃えます」）**: 対象を「高裁判決の根拠」の9行だけでなく、裁判文書ページの**全30行**（`裁判所`・`被告（ＥＮＥＯＳ）側`・`原告（通報者）側`の書面の行を含む）に広げた。`14-doc-accordion.css`: 行のタイトル文字（`summary`）の左右パディング0.2remを外し、タイトルも行の端に揃えた。`.doc-acc[data-plain] > .doc-body` の限定を外し、`.doc-acc > .doc-body` 全行を対象にした。実測（1280px・375px・ダークモード）: 全30行でタイトル・本文・開閉マーク（シェブロン）とも行の左右端に一致（横溢れ0）。サイドノート（`.sn-note`）が本文列の右外へ出るのは意図どおりの設計（float:right）で、対象外。**`.card-accordion`（styleguide の見本1件。枠線・背景つきのボタン型で、実際のページでは未使用）は触っていない**: 文字を枠の端まで詰めるとボタンとして窮屈に見えるため。もとは入れ子の `.card-accordion` の中だけに効かせていたのを、行（`.doc-acc`）の中にも効くようにした（`05-card.css`）。判決文などの引用（`blockquote`。理由１・理由２の行）は、見出しを「高裁判決から引用」（判決文の該当箇所へのリンク）とし、文字は `small`（13px）にしてある（`14-doc-accordion.css`。サイト共通の `.md-content p` が 16px を指定しているので、枠だけでなく中の `p`・`li` にも指定した。引用でない地の文は 16px のまま。タイムラインの引用 `.tl-quote` と「関係法令・規程」の抜粋は、そのまま）
- **「判決の概要」は、アコーディオンの外、ページの上（一覧の先頭。「高裁判決の根拠」の欄の前）に置いている**（2026-09-21。今は `docs/trial2024/index.md`）。2026-09-24、見出し（`.issue-lead.sec-title`）の3行目に `<span class="smaller">（AIによる解説、2026.09）</span>` を足した。同日、`docs/trial2021/index.md` にも同じ形の「2021年提訴 判決の概要」を置いた（Claude が判決文だけから作成。同じ表記付き）。一覧と同じ `toc-wrap doc-list` の中にあるので、見出し（`##`）も本文も、一覧と同じ列（32rem）にそろう。もとは「判決の概要と分析」の冒頭にあったが、上に出したので、そちらからは削除した（文はこの1か所だけ）。そこにあった導入「以下は、２つの理由の該当箇所を判決文から抜粋しました。」は、抜粋の行の見出しがあるので入れていない
- 旧 `trial/judgement_2025/`（判決の概要と分析のページ）は、「高裁判決の根拠」の見出し（`id="bunseki"`。旧称は「判決文の答え合わせ」。`14-doc-accordion.css` の `.doc-side[id]` で固定ヘッダーに隠れないようにしてある）へ飛ばす転送ページ（`redirect_to: trial/#bunseki`、`overrides/main.html`）にした。2026-09-23 の改名で、今は `docs/trial2024/judgement_2025.md`（`redirect_to: trial2024/#bunseki`）。旧 `docs/trial/index.md` も `trial2024/` への転送ページ。ナビの「判決書」は外した。判決文はここ（裁判文書ページ）にだけある
- 旧 `trial/eneos/`・`trial/whistleblower/` は持たない。旧サイト（`/hotline/`）の URL であって、新サイトには存在しなかったため
- **Python フック（`doc_indent.py`・`add_blog_class.py`）とプラグイン（`mkdocs-glightbox`）を無くした**。やっていたことは、ソースへの焼き込みと、ページ内のスクリプト・CSS に置き換えた
  - 独自マーカー `:N X#id:` → 本文ファイルに `<p class="padN …">` として焼き込み済み
  - `:include:` → `--8<-- "パス"`（標準の拡張 `pymdownx.snippets`）
  - `body.trial-doc` → CSS の `body:has(.trial-doc-marker)`（各ページ先頭の目印 `<div class="trial-doc-marker" hidden></div>`）
  - agm・見本帳への Swiper 注入 → そのページの md に `<link>`・`<script>` を直接書いた
  - 画像クリックで拡大 → `docs/vendor/glightbox/`（同梱）と `docs/js/lightbox.js`
- `deploy.bat`・空の `includes/sitemap.md` は持ち込んでいない

## 書面の追加・更新

本文は**サイドノートアプリ**（`C:\minnanosaiban\sidenote`。旧 sidenote-pdf-web は2026-09に統合・廃止済み）で作る。

1. アプリの「hotline用」ボタンで書き出す。`<p class="pad1 idt">` のような生HTML＋ノートは直後の `<aside class="sn-note">` になり、対応ブラウザ（Chrome/Edge）では選んだフォルダ（`docs/trial2024/md` 等）へファイル名だけでそのまま書き込める（次回以降は記憶したフォルダに書くだけ）。非対応ブラウザでは `.md.txt` としてダウンロードされるので、`docs/trial2024/md/<id>.md.txt`（2021年提訴の書面なら `docs/trial2021/md/`）に貼る（既にあるファイルなら上書き）
   - 拡張子を `.md.txt` にしているのは、ビルダーが独立したページにしてしまわないようにするため
   - 相互参照リンク・認否ノートの装飾（バッジ等）・アンカー（`<a name>`）はこの書き出しには含まれない。従来通り `import_argument.py` か手作業で追加する
2. PDF があるときは、`python scripts/add_pdf.py …` で `docs/pdf/` に入れる（下の「PDF」。PDF 一覧ページも自動で作り直される）
3. 書面を増やすときだけ、提訴年のページ（`docs/trial2024/index.md` など）の該当する欄（裁判所・被告（ＥＮＥＯＳ）側・原告（通報者）側）の `<div class="doc-rows" markdown>` に、次のブロックを1つ足す

```html
<details class="doc-acc" id="<id>" data-md="md/<id>.md.txt" data-pdf="../pdf/<ファイル名>.pdf" data-summary="<要約>" markdown>
<summary>書面名</summary>
<div class="doc-body" markdown>

--8<-- "docs/trial2024/md/<id>.md.txt"

</div>
</details>
```

`data-pdf`・`data-summary` は無ければ書かない（ボタンがグレーになる）。ボタン・開閉マーク・サイドノートの位置は `docs/js/doc-accordion.js` が付ける。
目次を出さない構成なので、貼った本文の見出しが目次に混ざる心配はない。

今の書面のうち、被告側7行と原告側の4行（原告第５準備書面・控訴理由書・控訴理由補充書（１）（２））と判決文2行は、元の `eneos.md`・`whistleblower.md`・`judgement_2025.md` を分割したもの。旧形式（hotline 用書き出し）のマーカーは HTML に焼き込み済みなので、
そのままで表示できる（`.md` ボタンの「コピー／ダウンロード」では、旧形式は素の Markdown に変換して渡す）。今後の書面は「ウェブ用」のままでよい。
要約（`data-summary`）は、判決文の行にだけ入っている（2024年提訴・2021年提訴の各2行。主文は判決文から、理由は各ページの「判決の概要」の文から）。`data-summary-model`・`data-summary-date` が無い要約には、ダイアログに「AI要約」の表示が付かない（`doc-accordion.js`）。今の4行はどれも付けていない。ただし2021年提訴の2行の要約は、2026-09-24 に Claude が判決文から書いたもの。

### 認否のサイドノート（`eneos-saiban` の `argument.md` から移した分）

`eneos-saiban/argument.md` の欄外メモ（`{margin}` ブロック。**原告の書面には被告の認否、被告の書面には原告の認否**）を、`scripts/import_argument.py` で取り込んだ（2026-09-21、一度きり。
すでにサイドノートのある書面には足さずに止まる）。同じ形（段落の直後の `<aside class="sn-note">`）なので、今後サイドノートアプリの「ウェブ用」書き出しを貼った書面と、同じ見た目・同じ動きになる。

- **ノートのある11書面**: 訴状、原告第１〜４準備書面、求釈明申立書、控訴理由書（＝原告側。被告の認否）、答弁書、被告準備書面（２）〜（４）（＝被告側。原告の認否）。ノートの数は、元の `{margin}` の数（261）と一致
- **被告準備書面（３）（４）（５）に、原告の認否を足した（2026-09-22。ユーザーの依頼）。これで被告側の書面のうち、認否を返す原告の書面があるものには、すべてノートが付いた**（ノートは 261 → 294 か所）。
  `argument.md` の側は、（３）が「← ＥＮＥＯＳの主張」の印だけ（7か所）、（４）が印だけ（3か所）、（５）は無しで、認否の本文は原告の書面の本文（原告第３準備書面 第２／原告第５準備書面 第３）にあるだけだった。それを、答弁書・被告準備書面（２）と同じ形（1行目にラベル「原告第○準備書面第○の○」、続けて「…」以下について、争う。／認める。／不知、原告の主張は…のとおり）で、被告の該当する段落の直後に足した。
  - **（３）に7か所**: 元の「← ＥＮＥＯＳの主張」の7か所と、原告第３準備書面 第２の7か所（１のア・イ・ウ、２のア・イ・ウ、３）が1対1。印の後ろに認否を続けた（答弁書の「印 → 認否」と同じ並び）
  - **（４）に3か所**: 原告第５準備書面 第３の１（第２の１ ⑴ エ と ⑵ イ の2か所に、同じ文）と２（第４の見出し）
  - **（５）に23か所**: 原告第５準備書面 第３の３〜１２。引用された段落ごとに1つ（第３の４ ⑴ ア は3段落なので3つ、⑷ ア の第１・第２段落は1つにまとめた）。第３の３（第２の１ 全体への「認める」）だけは、段落でなく見出しに付けた
  - 文は、元の書面（`dai3.md.txt`・`dai5.md.txt`）の段落から機械的に取り出して、一字一句一致することを確かめた（説明の段落は丸ごと。脚注の印は、既存のノートと同じく「（注２１）」「※３２」の平文）。「同第○」は、ノートが被告の書面の中にあるので、場所の言い方を短くした
  - **原文の誤記は、そのまま写してある**（元の原告の書面がそうなっている）: 「内部統制基本法氏は」（被告の書面は「内部統制基本方針は」）、「各規程の」（被告の書面は「各規定」）、「ついては、争う」（→「については」）、「行動基準は、行動基準は、」（原告第３準備書面）。直すなら、原告の書面の本文（`dai3.md.txt`・`dai5.md.txt`）とノートの両方を
  - 同時に、取り込み（`scripts/import_argument.py`）で付き先を間違えていたノートを3か所直した: ① 被告準備書面（４）の「← ＥＮＥＯＳの主張」の2つ目が、同じ文の ⑵ イ でなく ⑴ エ の段落に重なっていた（⑵ イ へ移した）。② 答弁書の先頭のノート（原告第１準備書面第３の１ ⑴）、③ 被告準備書面（２）のノート（原告第２準備書面第２の１ ⑵）が、見出しと同じ文の**目次の項目**に付いていた（本文の見出しの直後へ移した）。原因は、取り込みが「最初に一致した段落」に付ける作り（目次が先にある・同じ文が2回ある）。**今後、`argument.md` から同じ形で取り込むときは、目次と本文で同じ見出しがあることに注意**
  - 検証: 追加後の5書面（答弁書・被告準備書面（２）〜（５））の本文は、ノートを除けば直前と同じ。ビルド後のページで、ノートは全て見えて、重ならず、ノートの中のリンク（28か所）に切れは無い。**確認できていないもの: 画面のスクリーンショット**（Claude の窓が隠れていて撮れなかった。DOM で位置・重なり・リンクを調べた）
- **原告側の7書面（訴状、原告第１〜４準備書面、求釈明申立書、文書送付嘱託申立書）は、この repo にまだ無かったので、`argument.md` の本文から起こした**（「原告第４準備書面以下」の外部リンクの行は、これに置き換えた）。
  書式は既存の書面に合わせて変換（インライン style → `doc-gap-top`・`smaller`、`base padN` の囲み → `pad(N-1)`、引用の枠 `card_01` → `card` など）。
  元で目次が「≪ 中略 ≫」になっていた書面（原告第１〜４準備書面）は、カードの目次と本文の見出しから目次を復元した。元の本文の末尾にあった単独の `##` を除き、文書送付嘱託申立書の重複アンカー `bunsyo_1`（2つ目）を `bunsyo_2` にした。これらの7書面の本文は、公開ビルドの `argument.html`（欄外のノートを除く）にそのまま含まれることを確かめた。PDF との突き合わせ（校正）はしない
- **すでにあった5書面（答弁書、被告準備書面（２）〜（４）、控訴理由書）の本文は、取り込みでは変えていない**（`argument.md` とは、1文字ほどの違い（「カ」と「力」、「令」と「今」など）が数か所あり、答弁書・被告準備書面の目次は「≪ 中略 ≫」になっていたので、本文は既存のものを正とし、ノートだけを差し込んだ）。ノートを除いた本文が、元と同じであることを確かめた（ビルド後の HTML でも、既存15書面の本文は取り込み前と同じ）。
  **控訴理由書だけは、取り込みのあとで `argument.html` に合わせて2か所を直した**（2026-09-21）: 「位置付けてる制度」→「位置付けている制度」、「一審判決の該当箇所」の枠の中の行頭にまぎれていた「> 」（ページに「>」が出ていた）を削除。直したあと、控訴理由書の本文は `argument.md` と完全に一致する
- ノートは、元は段落の**直前**に置かれていたので、対応する段落の**直後**へ回した（`doc-accordion.js` が、その段落の頭の位置へ寄せて、参照している行と揃える）。連続する複数のノートは、順番のまま同じ段落に付く。
  「← ＥＮＥＯＳの主張」の印は、元は囲み（div）の頭に付いていたが、その囲みの最初の段落に付けた
- 赤字の「争う」「否認」は `.strong-rd`、「← ＥＮＥＯＳの主張」のバッジは `.sn-badge`（`14-doc-accordion.css`）。ノートの中の「下記 ⑷」などのリンクは、このページの別の書面の位置（`#dai1_314` など）へ飛び、閉じている書面は開く
- **`.md` ボタン**: ノートは「> 」で始まる引用として、対応する段落の直後に出る
- **「●」は、表示ではアイコン `<i class="bi bi-dot"></i>` に置き換えてある**（2026-09-21。書面の本文の箇条書き66か所とノート181か所。ソースの `docs/trial/md/*.md.txt`（今の `docs/trial2024/md/`）の中で置き換えた）。`.md` ボタンでは元の「●」に戻して渡す（`doc-accordion.js` の `DOT_ICON_RE`）。ぶら下げの字下げ（`text-indent: -1em`）がアイコンの幅を打ち消して 0 にしてしまうので、`14-doc-accordion.css` に `.doc-body i.bi { text-indent: 0 }` を入れてある。ページの文字としては「●」でなくなるので、ページ内検索やコピーには出ない。**今後、サイドノートアプリの書き出しを貼った書面に「●」があれば、貼ったあとに同じ置き換えが要る**
- **幅 1220px 以下（スマホ・タブレット）では、ノートを出さない**（これまでのサイドノートと同じ方針で、スマホでは出さないことに決めた。`14-doc-accordion.css`）

## PDF（`docs/pdf/`）

書面・判決文・証拠の PDF は、この repo の `docs/pdf/` に置く（もとは `eneos-saiban` の `_static/` にあった。26本、合計 約20MB。`eneos-saiban` の元ファイルには手を付けていない）。
内訳は、裁判文書ページの20行分（書面18・判決文2）、判決ページが引用する証拠（甲号証）6本（甲17・19・20・21・25・26）。

- **ファイル名と PDF の「タイトル」（メタデータ）を、検索されやすい同じ名前にそろえる**:
  `ENEOS（エネオス）の内部通報制度をめぐる訴訟について――ENEOS側_2024年04月15日_答弁書.pdf`（区分は `ENEOS側`・`通報者側`・`裁判所`）。
  検索結果に出る PDF のタイトルは、このメタデータが使われる。区切りのダッシュ「――」（U+2015 を2つ）は `scripts/add_pdf.py` の `SEP` 1か所。日付は書面の冒頭にある日付（判決は言渡日）。
  **証拠（甲号証）は、日付の代わりに証拠番号**を入れる: `ENEOS（エネオス）の内部通報制度をめぐる訴訟について――通報者側_甲17_税務処理の確認経緯.pdf`（甲号証は原告＝通報者側の提出なので `通報者側`）
- **軽量化（判決文2本）**: 元は 38MB と 92MB だった。1ページが 1653×2337pt（A4 の約2.8倍）の大きさで、4591×6491 画素のカラーPNG（約3000万画素、約550dpi 相当）
  だったため。**A4・200dpi・1bit に作り直して、地裁 1.5MB・高裁 1.7MB** にした（もとが白黒のスキャンなので、文字・印影・黒塗りは鮮明なまま。紙の質感のノイズだけ消える）。
  理由は、Googlebot が読む PDF は**先頭 64MB まで**（92MB では後半が検索の対象から外れる）、GitHub Pages の転送量の目安が**月 100GB**（92MB だと約1,000回のダウンロードで到達）、閲覧者のダウンロード時間
- **文字層（OCR）**: 画像だけだった9本（判決文2本・答弁書・被告準備書面（１）〜（５）・控訴答弁書、計150ページ）に、scan-ocr（`C:\minnanosaiban\scan-ocr`、エンジンは YomiToku）で透明な文字層を付けた。
  画像は元のまま、その上に文字を重ねている（scan-ocr の「テキスト乗せPDF」は画像を JPEG で作り直して大きくなるので、認識結果だけを使った）。
  サイトの検証済み本文と比べた一致（5文字連続の再現率）は 92.8〜97.7%（判決文は 96.8%・97.7%）。MuPDF・PDFium（Chrome）・pypdf の3つで、同じ文字数が取り出せる。
  OCR なので読み違いは残る（例: 「ワ」→「八」）。文字層は検索とコピー用で、見た目には出ない。原告側の4本は、もともと文字情報があった
- **PDF 一覧ページ**（`docs/pdf/index.md`、URL は `/pdf/`。ヘッダーのタブには出さず、裁判文書ページの冒頭のリンクから行く）: PDF へのリンクが JavaScript を使わずに HTML に入るので、検索エンジンが PDF を見つけやすい。
  区分ごと（裁判所・被告（ＥＮＥＯＳ）側・原告（通報者）側・その証拠）に、書面名・日付・ページ数・大きさを並べ、本文の行がある書面には「本文」のリンクが付く。**`scripts/pdf_index.py` が `docs/pdf/` の中身から作る**（手では直さない）。`add_pdf.py` が PDF を足すたびに作り直す。nav に入れていないので、Zensical のサイトマップには載らない（MkDocs は載せる）。検索エンジンは、裁判文書ページ（サイトマップにある）のリンクからたどる
- **追加のしかた**: `python scripts/add_pdf.py <元のPDF> <ENEOS側|通報者側|裁判所> <YYYY-MM-DD|甲17> <書面名> [--shrink] [--ocr]`（`pip install pymupdf`。`--shrink`・`--ocr` は `scripts/pdf_tools.py` を使い、Pillow と scan-ocr の環境が要る。サイトのビルドには要らない）。
  `--shrink` は画像だけの白黒PDFを A4・200dpi・1bit に（色つきは断る）、`--ocr` は文字情報のないPDFに文字層を付ける（1ページ約20秒、CPUだけで動く）。最後に出る `../pdf/<ファイル名>` を `data-pdf` に書く
- 元の PDF にページ回転（180°・270°）が付いている場合は、文字層を重ねる前に、見た目を変えずに取り除く（回転したままだと、文字の向きと位置がずれる）
- PDF は Git ではバイナリ扱い（`.gitattributes` の `*.pdf binary`）
- 確認したこと: 13本とも、ページ数とタイトル（=ファイル名）が合っている。縮小していない11本は、元の PDF と描画が画素まで同じ。縮小した2本は、白黒化による差だけ（平均 5/255 以下）

## 検索エンジン向けの設定（「ENEOS 通報 裁判」などで見つかるように）

狙う検索語: 「ENEOS／エネオス」×「通報」「通報 裁判」「通報 訴訟」。

- **`<title>`**: 各ページ先頭の front matter `seo_title:` がそのまま `<title>` になる（`overrides/main.html`）。検索語を先頭に置く。
  `title:` の方はヘッダーの表示や `og:title` に使われるので、別にしてある。`seo_title` が無いページは「ページ名 - サイト名」
- **説明文**: front matter の `description:`（`<meta name="description">` と `og:description`）。ページごとに違う文にする
- 全角の「ＥＮＥＯＳ」は、検索エンジンが半角と同じに扱う。それでも `<title>`・説明文・フッターでは、半角の「ENEOS」と「エネオス」を併記している
- **フッターの1文**（`overrides/partials/copyright.html`）: 全ページの本文に「ENEOS（エネオス）」「通報」「裁判・訴訟」が入る
- **検索結果に出したくないページ**: front matter に `robots: noindex, nofollow`（見本帳に付けてある）
- **サイトマップ**: ビルドで `sitemap.xml` ができる（Zensical は nav にあるページだけ。MkDocs は全ページなので見本帳も入るが、noindex なので害はない）
- `docs/robots.txt` は、**サブパス（`/hotline/`）に置いても検索エンジンは読まない**（読まれるのはホスト直下だけ）。害はないので残してある。
  サイトマップは Search Console から送る
- **Googlebot が読む HTML は先頭 2MB まで**（公式ドキュメント）。裁判文書ページ（`trial2024/index.html`）は 約860KB（2026-09-24 のビルド。以前の記録は約470KB）。書面を増やして 2MB に近づいたら、ページを分ける
- 効果が出るまでは、Search Console の登録と、サイトマップの送信が要る（下の「公開後にやること」）

## 公開後にやること

1. **Google Search Console**: 「URL プレフィックス」でプロパティ `https://minnanosaiban.github.io/hotline/` を追加して所有権を確認する。
   HTML ファイル方式の確認用ファイル（`googlee01c….html`）はこの repo にも入れてあるが、通らなければ Search Console が出す新しいファイルを `docs/` に置いて push する。
   その後、サイトマップ `sitemap.xml` を送り、各ページを「URL 検査 > インデックス登録をリクエスト」する
2. **Bing Webmaster Tools**: サイトを追加する（`msvalidate.01` の meta は `overrides/main.html` に入っている）。Search Console からのインポートも使える
3. **IndexNow**（Bing・Yandex などへの新規・更新の通知）: 公開後に `build.bat` でサイトマップを作り、`powershell -ExecutionPolicy Bypass -File scripts\indexnow_ping.ps1`
4. 旧サイト（`/hotline/`）に同じ本文が残っている間は、検索エンジンがどちらを正とみなすか割れる。旧サイトから新サイトへ `rel="canonical"`
   （または転送）を張れば確実（旧 `hotline` リポジトリの変更が要るので、この repo では行っていない）

## 設計ルール（Zensical と MkDocs の両方で動かすための落とし穴）

- **フック・プラグインを足さない**。Zensical は Python フックも MkDocs プラグインも読まない。処理が要るなら、ソースへの焼き込みか、ページ内の JS/CSS で
- **`mkdocs.yml` に `watch:` を書かない**。`custom_dir` と併用すると、Zensical は何も出力しない（エラーも出ない）
- **raw HTML の相対パス（`<a href>`・`<img src>`・`<script src>`・`<link href>`）は、ビルダーで解釈が違う**。Zensical はソースファイルの位置基準、MkDocs は書き換えず URL 基準。
  両方で同じ意味になるのは `フォルダ/index.md` 形式のページ（`agm/`・`trial2021/`・`trial2024/`・`.trial2026/`・`styleguide/`・`about/`）だけ。`docs/xxx.md` 直下のページで `../` を使うと壊れる。
  `index.md` でないページ（今は転送だけの `trial2024/judgement_2025.md`）では、Markdown リンクは `index.md#id` 形式、raw HTML には絶対パスを使う。取り込み用ファイル（`docs/trial2024/.parts/`・`docs/trial2024/md/` など）の中のリンクは、**取り込み先のページから見た**書き方にする（`trial2024/index.md` に取り込むなら、`#id` や `../pdf/…`）
  （`judgement_2025.md` を `judgement_2025/index.md` にすると、Material の `navigation.indexes` が節の見出しページ扱いにして、ナビからその行が消える）
- **`**強調**` が全角の句読点・括弧に隣接するとき、Zensical では強調にならない**ことがある（`pymdownx.betterem` の挙動差）。そういう箇所は `<strong>…</strong>` と書く
- 裁判文書系のページ（`trial2021/`・`trial2024/`・`.trial2026/`・`agm/`・`styleguide/`）は、先頭に `<div class="trial-doc-marker" hidden></div>` を置く。CSS が `body:has(.trial-doc-marker)` でページを見分けている
- サイドノートは画面幅 76.1875em 以下では出さない（本文列の外側の余白に置くため）
- 各グループ（`.doc-rows`）の最後の行の下の罫線は、`details.doc-acc` の `border: none` に詳細度で負けるので、`14-doc-accordion.css` で詳細度を上げて出している（これが無いと、最後の行が `details` のグループには閉じの罫線が出ない）
- アコーディオンの `summary` には `overflow: visible` が要る（`14-doc-accordion.css`）。テーマの `summary` は `overflow: hidden` で、`.md` ボタンのメニューが行の高さで切れてしまう

## 検証（Zensical 0.0.63 と MkDocs 1.6.1）

同じ条件（1280×800、同じ devicePixelRatio、同一オリジンの iframe）で、旧構成（フック・プラグイン有り）・MkDocs（フック無し）・Zensical の3つをビルドして、全要素の位置・サイズ・主要スタイル20項目を比べた。

- 旧構成 対 MkDocs（フック無し）: Home・agm・書面一覧・判決・見本帳の**記事本文が全要素一致**。ヘッダー・タブ・フッターも一致
- MkDocs 対 Zensical: 記事本文は全要素一致。差は次の3点だけ（いずれも実害なし）
  - サイドノートの幅が最大 7px 狭い（Zensical のクラシックテーマが `html { scrollbar-gutter: stable }` を付けるので、`100vw` からスクロールバー分が引かれる）
  - `<a href="">`（MkDocs は `href="."`）、`<link rel="next">` の追加、`search.json` の出力（検索窓は無いので使われない）
  - サイドバーの入れ子構造が違う（デスクトップでは非表示。スマホの引き出しの項目とリンク先は同じ）
- 機能（両ビルドで同じ結果）: 12書面すべての `.md.txt` が取得できる／`.md` のコピー・ダウンロード／要約ダイアログ／色の切り替え／
  引用リンク4本が該当書面を自動で開いて見出し直下（110px）に着地／agm のカルーセルと画像拡大／スマホ幅のメニュー／目次・検索が出ない／内部リンクの 404 なし
- 本文テキストは、現行サイトの該当セクションと文字数まで一致（Home 1824・agm 3813・書面一覧 96875・判決 39656・見本帳 1773）

## URL・ドメインを変えるとき直す場所

- `mkdocs.yml`: `site_url`、`extra.about_url`
- 各ページ先頭の `url:`・`image:`（OGP）と、シェアボタンの `https://twitter.com/share?url=…`: `docs/index.md`・`docs/agm/index.md`・`docs/trial2021/index.md`・`docs/trial2024/index.md`・`docs/.trial2026/index.md`（非公開中）・`docs/about/index.md`
- PDF 一覧ページの `url:`・シェアボタンの URL は `scripts/pdf_index.py` の `BASE` から作られる（変えたら `python scripts/pdf_index.py`）
- `scripts/indexnow_ping.ps1` の `$keyLocation`
- `docs/robots.txt`（`Sitemap:`）、`docs/e482d7edf83b50b925f361e389d57812.txt`（IndexNow のキー。`scripts/indexnow_ping.ps1` が使う）、`docs/googlee01c6dd3b7b5851f.html`（Search Console の所有確認）
- `docs/styleguide/index.md` の見本リンク

## その他のファイル

- `docs/trial2024/.parts/bunseki-*.md.txt`（2本。`bunseki-nakatta`・`bunseki-goui`）: 「高裁判決の根拠」の2行の本文。`--8<--` で取り込むだけの部品。`.md` ボタンの元にする書面の本文は `docs/trial2024/md/` に置く
- `docs/trial2024/.parts/horei-*.md.txt`（4本。2026-09-22 新設）: 「関係法令・規程、証拠」の各行の本文。指針の解説・ＥＮＥＯＳ規程の2本は旧 `bunseki-horei.md` の分割、ハンドブック・ガバナンスコードの2本は新規。2026-09-23、`parts/` を `docs/` の外から `docs/trial2024/parts/` へ移し、拡張子を `.md.txt` に変更（`docs/trial2024/md/` と同じ理由: `.md` だと単独ページとして公開されてしまう）。ただし `docs/` の中の普通のフォルダ名だと生ファイルとしては公開されてしまう（Zensical 0.0.63 が `exclude_docs` を読まないため）ことが分かり、同日中にフォルダ名を `.parts/`（ドット始まり）へ変更。Zensical・MkDocs とも、ドット始まりのファイル/フォルダは site/ にコピーしない（実験で確認済み。`--8<--` 取り込み自体には影響しない）ため、これで完全に非公開へ戻った
- `deploy.bat`・`scripts/wait_deploy.ps1`: 公開（上の「公開（GitHub Pages）」）。`serve.bat`・`build.bat`: 見る・ビルドする
- `scripts/add_pdf.py`・`scripts/pdf_tools.py`・`scripts/pdf_index.py`: PDF を `docs/pdf/` に、検索されやすい名前とタイトルで入れる。軽量化・OCR・一覧ページ（上の「PDF」）
- `scripts/extract_agm_panels.py`: agm のスライド画像の切り出し（元の hotline から）
- `DESIGN_SYSTEM.md`: 元の hotline のデザイン仕様。フック・ブログ・プラグインに関する記述は、この repo には当てはまらない

## 未対応・要判断

- **運営者ページ**: フッターのリンク先（`extra.about_url`）は `tomo`（`https://minnanosaiban.github.io/tomo/`）に決まった。このサイトの `about/`（2026-09-23 追加。`tomo` の Home を写した並行運用版、noindex）を残すか、どちらかに寄せるかは未定
- **旧サイトとの重複**: 旧 `/hotline/` に同じ本文が残っている（上の「公開後にやること」4）
- **`eneos-saiban` にまだ頼っているもの**: `dai5` 本文中の ChatGPT ページへのリンクだけ。nav の「主張書面全文と認否」（`argument.html`）は 2026-09-22 に外した（本文と認否のノートは裁判文書ページへ移し済み）。甲8-16 の PDF は `eneos-saiban` にも存在しないが、そこへのリンク（旧タイムラインの行）は、行ごと 2026-09-22 に削除されたので、今はリンク切れは無い。元のファイルが見つかれば `add_pdf.py` で移せる
- `2026年提訴`（`docs/.trial2026/index.md`）は、中身が「このページは作成中です」のプレースホルダーと表示確認用の見本行だけだったため、2026-09-26 に当面非公開にした。フォルダ名を `trial2026/` → `.trial2026/`（ドット始まり）へ改名し、`mkdocs.yml` の nav と `trial2024/index.md`・`trial2021/index.md` の年別リンク（`trial-year-nav`）から外した（`.parts/` と同じ理由・同じ方法。上の「その他のファイル」参照。`git mv` で履歴は保持）。公開を再開するときは、フォルダ名を `trial2026/` に戻し、nav とリンクを元に戻す。最後に出ている「表示確認用（見本・削除可）」の行（`sample-web`）は、公開前に消してよければ `docs/trial2024/md/sample-web.md.txt` と併せて削除する
- 見本帳（`styleguide/index.md`）は、音声カードや `.repo-link` などの部品例を含んだまま（音声用のCSSは `05-card.css` に残っている）
- `mkdocs.yml` に、使っていない設定のコメントアウトが多く残っている（元のまま）
